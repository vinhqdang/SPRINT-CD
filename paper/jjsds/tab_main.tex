\begin{table}[t]
\centering
\caption{CERT-CD's own two error types: probability of ever making an unsound
assertion at any point in a monitored run, at $\alpha = 0.1$, $d = 5$, Laplace
noise, $n \le 3000$, with Wilson $95\%$ intervals. Parenthesised counts are
numbers of \emph{instances} in each stratum, not sample sizes. The budget is
split evenly, so each column is bounded by $\alpha_A = \alpha_D = 0.05$, not by
$0.1$. Both quantities are trajectory-worst.}\label{tab:main}
\footnotesize
\begin{tabular}{@{}lcc@{}}
\toprule
& \multicolumn{2}{c}{CERT-CD} \\
\cmidrule(lr){2-3}
stratum & false edge & wrong arrow \\
\midrule
$\delta$-strong faithfulness       & $0.000$          & $0.008$ \\
holds (1000 instances)             & \tiny{[.000,.004]} & \tiny{[.004,.016]} \\[3pt]
$\delta$-strong faithfulness       & $0.012$          & $\mathbf{0.058}$ \\
fails (500 instances)              & \tiny{[.006,.026]} & \tiny{[.041,.082]} \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{The comparators' own error type on the same instances as
Table~\ref{tab:main}: probability of ever losing a true edge at any point in
the monitored run (trajectory-worst), same setting and Wilson intervals.
SPRINT-CD and PC both decide absence by failing to reject, so a lost edge is
their analogue of an unsound assertion; CERT-CD never deletes, so it has no
entry in this table and Table~\ref{tab:main} has none in this one. The two
tables measure different failure modes on a shared set of instances---they
are not meant to be read column-against-column, and no ratio taken across
them is a validity comparison; only the within-table pattern by stratum
supports one.}\label{tab:main-comparators}
\footnotesize
\begin{tabular}{@{}lcc@{}}
\toprule
stratum & SPRINT-CD lost edge & PC lost edge \\
\midrule
$\delta$-strong faithfulness holds (1000 instances) & $0.000$ & $0.008$ \\
                                                    & \tiny{[.000,.004]} & \tiny{[.004,.016]} \\[3pt]
$\delta$-strong faithfulness fails (500 instances) & $0.338$ & $0.482$ \\
                                                    & \tiny{[.298,.381]} & \tiny{[.439,.526]} \\
\bottomrule
\end{tabular}
\end{table}
